# postgresql_streaming_replication
configuration of postgres streamming replication between 2 VMs

for this setup you need two working VMS, network connections, postgresql installed inside those vms, SSH tool (optional), root/sudo access.
